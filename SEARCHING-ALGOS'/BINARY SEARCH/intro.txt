//CONDENSING BINARY SEARCH IN RECURSIVE STYLE 
Algo binary(){
int n,i; ==> # of elements in an array two[100][100] ==> 0 <= n, i <= (+ Infinity)

//OuterForloop ==> O(n) ==> (ROWS) 
for(i=0; i<=n ;i++){
//InnerForloop ==> O(n) ==> (COLS) 
for(j=0; j<=n ;j++){
read(two[i][j]); // 2x2 matrix
 }//EOF 1st for loop
}//EOF OF PROGRAM
